<?php

// ログアウト処理

// 結論としては、
// あなたの今回の用途
// （攻撃・異常検知後に強制ログアウトさせるためのログアウトページ）
// なら、

// 「1. HTMLは出さずに →、セッション廃棄、 ログインページにリダイレクト」
// をメインに採用するのが一番すっきりしていて適切です。

// 理由・整理はこんな感じです：

// このログアウトは「ユーザーが自分で押したログアウトボタン」ではなく、
// 「セキュリティ上の理由で強制終了するためのページ」です。
// その場合、役割は
// セッションを安全に破棄する
// 改めて正規の入り口（ログインページ）に戻す
// の二つだけで十分で、中間に画面を挟まず即リダイレクトで問題ありません。
// 「お手数ですが再ログインを…」というメッセージ自体は良いのですが、
// それはむしろログインページ側で表示した方が一貫します。
// 例）クエリパラメータやフラグを渡して、ログインページで
// 「前回のセッションは安全のため終了しました。お手数ですが再ログインをお願いします。」
// と表示する。
// 一方で：

// 「ユーザーが自分で[ログアウト]リンクをクリックしたとき用」の通常ログアウトを別に用意するなら、
// そのときは「ログアウトしました → ログイン画面へのリンク」のようなHTMLを表示する設計もよく使われます。
// まとめると、

// 今の「レート制限・CSRF検知から呼ばれる強制ログアウト用ページ」は
// → セッション破棄だけ行い、すぐにログインページへリダイレクト。
// メッセージを見せたい場合は、ログインページ側で「安全のためログアウトしました」と出す、という構成がおすすめです。

// PHP のリダイレクトは Location ヘッダで URL を指定するだけなので、
// クエリパラメータを付ければ「データをURLに乗せて」渡せます。


// 例: 強制ログアウト理由を渡す
// $params = [
//     'status' => 'forced_logout',
//     'reason' => 'security',
// ];

// $query = http_build_query($params);
// header('Location: login.php?' . $query);
// exit;


// ログインページ側では、例えば
// $status = $_GET['status'] ?? null;
// $reason = $_GET['reason'] ?? null;

// のように受け取って、メッセージ表示に使えます。

http_response_code(429); // Too Many Requests
?>

<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Access Blocked</h1>
    <p>Your access to this page has been blocked.</p>
</body>
</html> -->