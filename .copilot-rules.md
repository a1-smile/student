# GitHub Copilot コード生成ルール

このファイルは GitHub Copilot にコード生成時の方針を伝えるためのものです。

## 基本ルール

- 品質、保守性、安全性を重視
- 可読性の高いコードを生成
- 再利用可能な設計を心がける
- テスタブルなコード構造
- SOLID原則に従う

## PHP 固有のルール

### セキュリティ要件
- **XSS対策**: `htmlspecialchars(値, ENT_QUOTES, 'UTF-8')` を使用
- **CSRF対策**: トークン検証を必須とする
- **SQL対策**: プリペアドステートメントを使用
- **入力検証**: すべての外部入力を検証
- **セッション管理**: セキュア属性付きクッキーを使用
- **パスワード管理**: `password_hash()` と `password_verify()` を使用
- **APIキー管理**: 環境変数または安全なストレージを使用

### エラーハンドリング
- SecurityException継承クラスを使用
- 適切なログ記録を実装
- ユーザーエラーメッセージを統一
- 外部APIやネットワーク通信は必ず失敗する可能性を考慮

### コードスタイル
- PSR-12準拠のコーディング規約
- PHPDoc必須
- 型宣言を使用

### 保守性
- 関数は単一責任原則に従う
- 依存性注入を使用
- コードの重複を避ける
- 変数名や関数名は意味のある名前を使用
- コメントでコードの意図を説明

## プロジェクト固有のルール

### 学生管理システム
- データベース操作は DBManager クラスを使用
- セッション管理は共通関数 `initializeSecureSession()` を使用
- エラー処理は exceptions.php の例外クラスを使用
- フォーム送信時は必ずCSRFトークンを検証

### ファイル構成
- 共通機能は common.php に集約
- セキュリティ関連は exceptions.php を使用
- データベース操作は適切なクラスに分離
